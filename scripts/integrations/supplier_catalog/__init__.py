"""Supplier catalog integration package.

Supplier-specific adapters normalize external structured catalog sources into
one internal, provider-neutral representation. This package does not own the
public PHP catalog or production database schema.
"""
