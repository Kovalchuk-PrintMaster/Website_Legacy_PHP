# Marketing schemas

Machine-readable marketing YAML is parsed as YAML 1.2 and then validated
against JSON Schema Draft 2020-12.

`marketing_control_plane_v0_1.schema.json` is the initial v0.1 control-plane
schema.

Schema validation complements semantic/referential validation; it does not
replace it.
