# Marketing programs

A program is a long-lived strategic stream that may own many campaigns,
reports, experiments, landing pages, and work items.

Program directories use stable IDs. Lifecycle state belongs in metadata, not
folder names.
