# Product extra optional tab v0.6.18f

Adds a fourth reserve product tab: `Додаткова інформація`.

DB fields:
- `tab_extra_enabled`
- `tab_extra_title`
- `tab_extra_content`

Default display mode is disabled (`Ні` / `0`).

Admin product service tab cards are compacted into a two-column layout.