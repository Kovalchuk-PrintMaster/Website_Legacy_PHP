# Версійні плани

- Current plan має статус `active`.
- При суттєвій зміні пріоритетів створюється нова версія.
- Completed plan стає `completed` або `superseded`.
- Фактичне виконання підтверджується code, checks і reports.
