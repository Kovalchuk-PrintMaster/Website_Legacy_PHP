# Raw Google Ads exports

Files in this directory are local evidence and are ignored by Git.

The versioned `raw-export-register.csv` records the semantic filename, original
uploaded filename, byte count and SHA-256 checksum.

Do not edit raw exports. Use the normalized files in the parent workspace for
campaign preparation and documentation.
